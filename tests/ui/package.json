{
  "name": "speechy-ui-tests",
  "version": "1.0.0",
  "description": "Playwright E2E tests for Speechy landing page",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
