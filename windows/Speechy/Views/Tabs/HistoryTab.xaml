<UserControl x:Class="Speechy.Views.Tabs.HistoryTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Empty state -->
        <StackPanel x:Name="EmptyState" Grid.Row="0"
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="Collapsed">
            <TextBlock Text="&#xE8F2;"
                       FontFamily="Segoe MDL2 Assets"
                       FontSize="50"
                       Foreground="#4D6B6B80"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,16" />
            <TextBlock Text="No recordings yet"
                       FontSize="16" FontWeight="SemiBold"
                       Foreground="#A0A0B8"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,4" />
            <TextBlock Text="Your transcriptions will appear here"
                       FontSize="12"
                       Foreground="#4D6B6B80"
                       HorizontalAlignment="Center" />
        </StackPanel>

        <!-- History list -->
        <ScrollViewer x:Name="HistoryScrollViewer" Grid.Row="0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Visibility="Visible">
            <StackPanel x:Name="HistoryList" Margin="16">
                <!-- History entries generated in code-behind -->
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border x:Name="FooterPanel" Grid.Row="1"
                BorderBrush="#1AFFFFFF" BorderThickness="0,1,0,0"
                Background="#1E1E2E"
                Visibility="Collapsed">
            <Grid Margin="16,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Item count -->
                <StackPanel Grid.Column="0" Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <TextBlock Text="&#xE8A5;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="12" Foreground="#A0A0B8"
                               VerticalAlignment="Center"
                               Margin="0,0,4,0" />
                    <TextBlock x:Name="ItemCountText"
                               FontSize="12" Foreground="#A0A0B8" />
                </StackPanel>

                <!-- Clear All button -->
                <Button Grid.Column="1" Click="ClearAll_Click"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="clearBorder"
                                    Background="#D9EF4444"
                                    CornerRadius="8"
                                    Padding="12,6">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE74D;"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="12" Foreground="White"
                                               VerticalAlignment="Center"
                                               Margin="0,0,6,0" />
                                    <TextBlock Text="Clear All"
                                               FontSize="12" FontWeight="Medium"
                                               Foreground="White" />
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="clearBorder" Property="Background" Value="#EF4444" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
